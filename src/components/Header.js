import React from 'react';
import logo from '../images/logo.png'

const Header = () => {
  return (
    <header>
        <div>
            <img src={logo} alt="logo" />
        </div>
    </header>
  )
}

export default Header
